**FROM:** B.U.G. · **TASK 01/11**

Management keeps losing tasks in email. Let’s build them a board. I believe in you, human. Please read that in my most encouraging voice.

**Your job**

1. Create and export an `App` component in the empty editor.
2. Return an `h1` that says **Sprint board**.
3. Write a short sentence in a `p` tag immediately after the `h1`. It will appear as the board's subtitle. Pick whatever you want; humans crave originality.

Press **Run → Start** (or **F5**) to see your board. I will supervise the pixels.
