**FROM:** B.U.G. · **TASK 02/12**

The company has three tasks and seventeen meetings about them. Reusable cards may reduce the meetings. I remain cautiously supportive.

**Your job**

1. Define `TaskCard` with a typed `title` prop.
2. Return a `li` containing a `p` that displays `{title}`.
3. In App, render three TaskCards inside a `ul`: **Plan sprint**, **Build board**, **Ship demo**.
4. Keep the **Sprint board** heading.

**Try it:** Change TaskCard once: all three cards should share the new markup.

**F1:** Components and props.
