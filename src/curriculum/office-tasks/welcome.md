**FROM:** B.U.G., Your Extremely Qualified Supervisor

**SUBJECT:** A slightly more personal welcome

Management has discovered that employees have names. Update our greeting to acknowledge this development.

- Export a function component with a capitalized name, such as `App`.
- Display a visible `h1` containing exactly **Welcome, Human**.
- Run your code and choose **Submit assignment** when the checks pass.

**Practice idea:** Store `Human` in a variable and insert it into your heading with JSX braces. Help now includes **JavaScript in JSX**. The checks accept any component implementation that produces the required heading.

Start from an empty file, or revisit an earlier task through **File → Open**. Opening a task restores its own saved draft.

> A personal touch. Please do not expect a personal salary review. — B.U.G.
