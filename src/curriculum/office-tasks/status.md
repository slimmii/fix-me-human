**FROM:** B.U.G., Your Extremely Qualified Supervisor

**SUBJECT:** Make our readiness official

Build the first version of the office status board.

- Export a function component with a capitalized name, such as `App`.
- Display a visible `h1` containing exactly **Office status: ready**.
- Run your code and choose **Submit assignment** when the checks pass.

**Practice idea:** Create a `Status` component that accepts a `status` prop. Render it from `App` with the value `ready`. Help now includes **Component props**, with TypeScript examples. The checks accept any component implementation that produces the required heading.

Your previous tasks and their saved code remain available in **File → Open**.

> If the board says ready, we can all stop asking each other. A historic productivity gain. — B.U.G.
