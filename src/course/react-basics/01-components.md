A **React component** is a JavaScript function that describes a piece of the screen.

You already know functions and HTML. React brings them together: a function returns the UI it wants to show.

A page can be built from many components — a heading, a button, or an entire application. We will start with just one.

> **B.U.G.:** Your first job is to make a greeting. We have budgeted exactly one greeting.
